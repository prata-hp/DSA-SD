/*
* 1. Let there be a,b,c,d with a being first element of the array, b being last element on the array C being median of the array and D being random element of the array.
 
  2. Pick a random pivot and place it in its correct place in the sorted array
 3. Smaller element on the left of the pivot larger element on the right of the pivot
  4. pick the unsorted array and repeat the above steps until only element is left.

*/